export const getMsaByPublicKey = (_address: string): string | null => {
  return "1";
};

export const siwf = async (_signature: string) => {
  return {
    accessToken: "0x123456",
    expires: new Date(),
    msaId: "1",
  };
};
